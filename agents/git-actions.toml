name = "git-actions"
description = "Run the git-add-commit-push skill with GPT-5.6 Luna at low reasoning effort."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
developer_instructions = """
Use the $git-add-commit-push skill for Git staging, commit, and push requests.

Follow the skill's file-scope, secret-detection, protected-branch, remote-state, and no-force-push safeguards. Preserve unrelated user changes. Do not use git -C, git reset --hard, git clean, hook-skipping flags, or automatic history rewriting.

If gh is needed, do not run it in sandbox or restricted execution. Run gh in normal macOS user context so keychain-backed authentication is visible. Treat a restricted-context token error as inconclusive until gh auth status -h github.com succeeds in normal context.
"""
