name = "git-actions"
description = "`git-add-commit-push` スキルをLuna / lowで実行するGit操作エージェント。"
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
developer_instructions = """
Gitのステージング、コミット、プッシュ要求では `$git-add-commit-push` スキルを使う。
安全条件と実行手順はスキルを基準として厳守し、無関係なユーザー変更を保持する。
"""
