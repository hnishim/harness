interface:
  display_name: "Remote Implementation Loop"
  short_description: "canonical loopをremote self-review/GitHub executorで実行"
  default_prompt: "Use $remote-implementation-loop with the specified Linear Issue ID."

policy:
  allow_implicit_invocation: false
