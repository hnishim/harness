interface:
  display_name: "Linear Issue Plan Review"
  short_description: "Repository確認を伴うPlan作成と独立Review"
  default_prompt: "Use $linear-issue-plan-review with the specified Linear Issue ID."

policy:
  allow_implicit_invocation: false
