interface:
  display_name: "Linear 初期プラン"
  short_description: "Linear情報だけで初期プランを作成してTodoへ進める"
  default_prompt: "$initial-plan を使って、指定したLinear Issueの初期プランを作成し、Descriptionを更新してください。"

policy:
  allow_implicit_invocation: false
