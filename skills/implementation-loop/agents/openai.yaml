interface:
  display_name: "Implementation Loop"
  short_description: "Route Linear phases for normal issues and spikes"
  default_prompt: "Use $implementation-loop with the specified Linear Issue ID. Route strictly from Linear Status, delegate Planning to $linear-issue-plan-review, then execute normal or Spike workflow from labels through review and explicit Close."

policy:
  allow_implicit_invocation: false
