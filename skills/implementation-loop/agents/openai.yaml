interface:
  display_name: "Implementation Loop"
  short_description: "Linear StatusからPlanning〜Closeまで進める単一入口"
  default_prompt: "Use $implementation-loop with the specified Linear Issue ID."

policy:
  allow_implicit_invocation: false
